/**
 * Card domain model (Technical Design §6).
 *
 * Placeholder for the scaffold. The full typed `LiquidCard` discriminated union,
 * `TemplateType`, `ChallengeCategory`, and related types are defined in the
 * card-schema task. Kept as a stub so the directory and import path exist.
 */
export {};
