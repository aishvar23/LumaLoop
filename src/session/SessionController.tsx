/**
 * SessionController (Technical Design §4, §8) — owns feed progression and stays
 * independent of template internals. Placeholder for the scaffold; the
 * controller is implemented in the session-engine task.
 */
export default function SessionController() {
  return null;
}
