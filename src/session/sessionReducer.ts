/**
 * Session reducer (Technical Design §8) — owns session progression and the
 * idle → active → resolving_card → completed transitions. Placeholder for the
 * scaffold; the reducer is implemented (with tests) in the session-engine task.
 */
export {};
