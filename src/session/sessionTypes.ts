/**
 * Session state types (Technical Design §8).
 * Placeholder for the scaffold; the `SessionState` / `SessionMode` types and
 * state-machine contract land with the session-engine task.
 */
export {};
