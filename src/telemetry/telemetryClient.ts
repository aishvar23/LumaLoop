/**
 * Telemetry client (Technical Design §10) — in-memory queue, immediate flush,
 * and a capped localStorage retry queue. Placeholder for the scaffold;
 * implemented (with tests) in the telemetry-client task.
 */
export {};
