/**
 * TemplateRenderer (Technical Design §7) — dispatches the active card to the
 * matching template renderer behind the shared `TemplateProps` /
 * `CardResolution` contract. Placeholder for the scaffold; dispatch and the
 * per-template renderers land in their own tasks.
 */
export default function TemplateRenderer() {
  return null;
}
