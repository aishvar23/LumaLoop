/**
 * FeedFrame — the vertical, full-bleed shell that hosts the active card
 * (Technical Design §4, §14). Placeholder for the scaffold; the feed controller
 * mounts cards into this frame in a later task.
 */
export default function FeedFrame() {
  return null;
}
