/**
 * SessionReceipt — end-of-session summary (Technical Design §9).
 * Placeholder for the scaffold; receipt computation and copy land in a later
 * task and must avoid trait/ability claims.
 */
export default function SessionReceipt() {
  return null;
}
